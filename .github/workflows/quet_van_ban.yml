name: Robot Quet Van Ban HSCV

on:
  schedule:
    - cron: '0 * * * *' # Chạy mỗi tiếng
  workflow_dispatch: # Nút bấm chạy tay

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Cai dat thu vien
        run: pip install -r requirements.txt
      - name: Chay Robot
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          GSPREAD_SERVICE_ACCOUNT: ${{ secrets.GSPREAD_SERVICE_ACCOUNT }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          # THÊM 2 DÒNG NÀY ĐỂ ROBOT THẤY TÀI KHOẢN:
          SKHCN_USER: ${{ secrets.SKHCN_USER }}
          SKHCN_PASS: ${{ secrets.SKHCN_PASS }}
        run: python robot.py
        - name: Chay Robot
  run: python robot.py 2>&1
  env:
    ...
